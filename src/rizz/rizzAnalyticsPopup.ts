/** Keystroke / menu entry point for the full-page Rizz Analytics app (see `rizzAnalyticsApp`). */
import {openRizzAnalyticsApp} from './rizzAnalyticsApp';

export function showRizzAnalyticsPopup() {
  openRizzAnalyticsApp();
}
